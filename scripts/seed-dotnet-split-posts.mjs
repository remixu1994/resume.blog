import Database from 'better-sqlite3';

const dbPath = process.argv[2] || 'data/blog.sqlite';
const updatedAt = '2026-06-09';

const oldCombinedPostIds = [
  'clr-and-csharp-type-system-objects-heap-and-garbage-collection',
  'thread-concurrency-and-synchronization-thread-costs-to-deadlock-governance',
  'dotnet-application-architecture-dependency-injection-mediatr-aop-and-resilience',
  'service-communication-and-authentication-from-tcp-wcf-to-grpc-and-jwt',
  'dotnet-engineering-toolchain-cli-and-package-management-practices',
];

const b = (lines) => lines.join('\n');

const posts = [
  {
    groupId: 'csharp-value-types-reference-types-and-object-semantics',
    tags: ['csharp', 'dotnet'],
    zhTitle: 'C# 值类型、引用类型与对象语义',
    enTitle: 'C# Value Types, Reference Types, and Object Semantics',
    zhSummary: '把 class、struct、object 放回类型语义里理解：什么时候表达值，什么时候表达对象身份。',
    enSummary: 'A focused explanation of class, struct, and object semantics: when to model values and when to model object identity.',
    zhBody: b([
      '# C# 值类型、引用类型与对象语义',
      '',
      '很多 C# 初学者会把 `class` 和 `struct` 简化成“一个在堆上，一个在栈上”。这个说法容易误导。真正重要的是：它们表达的语义不同。',
      '',
      '值类型更像一个值：坐标、金额、范围、颜色。引用类型更像一个对象：订单、用户、连接、服务实例。',
      '',
      '## 三个差异',
      '',
      '- 存储方式：值类型通常内联在宿主位置，引用类型变量保存对象引用。',
      '- 复制语义：值类型赋值更像复制值，引用类型赋值更像共享同一个对象。',
      '- 生命周期：引用类型对象通常由 GC 管理，值类型通常跟随宿主对象或栈帧。',
      '',
      '```csharp',
      'public readonly struct Money',
      '{',
      '    public decimal Amount { get; }',
      '    public string Currency { get; }',
      '}',
      '',
      'public sealed class Order',
      '{',
      '    public Guid Id { get; } = Guid.NewGuid();',
      '    public Money Total { get; init; }',
      '}',
      '```',
      '',
      '## 选择建议',
      '',
      '如果数据很小、不可变、没有独立身份，可以考虑值类型。如果对象有身份、状态变化、继承或共享语义，就应该使用引用类型。',
      '',
      '这不是语法选择，而是建模选择。',
    ]),
    enBody: b([
      '# C# Value Types, Reference Types, and Object Semantics',
      '',
      'It is easy to describe `class` and `struct` as “heap versus stack,” but that explanation is incomplete. The more important distinction is semantic.',
      '',
      'A value type represents a value: a coordinate, amount, range, or color. A reference type represents an object with identity: an order, user, connection, or service instance.',
      '',
      '## Three Differences',
      '',
      '- Storage: value types are often stored inline with their owner, while reference variables point to objects.',
      '- Copy semantics: assigning a value type copies the value; assigning a reference type shares the object reference.',
      '- Lifetime: reference objects are usually managed by GC; value types often live with their owner or stack frame.',
      '',
      '## Choosing Between Them',
      '',
      'Use value types for small, immutable, identity-free data. Use reference types when identity, state changes, inheritance, or sharing matter.',
    ]),
  },
  {
    groupId: 'dotnet-managed-heap-and-garbage-collection',
    tags: ['dotnet', 'csharp'],
    zhTitle: '.NET 托管堆与垃圾回收：对象生命周期怎么被管理',
    enTitle: '.NET Managed Heap and Garbage Collection: How Object Lifetime Is Managed',
    zhSummary: '从对象分配、可达性、代际回收到 IDisposable，理解 GC 解决什么，也不解决什么。',
    enSummary: 'From allocation and reachability to generational collection and IDisposable, understand what GC solves and what it does not solve.',
    zhBody: b([
      '# .NET 托管堆与垃圾回收：对象生命周期怎么被管理',
      '',
      '`new` 不只是创建实例，它会让 CLR 在托管堆上分配对象。对象分配通常很快，但回收、压缩和晋升都需要运行时付出成本。',
      '',
      '```mermaid',
      'flowchart TB',
      '    A["new 对象"] --> B["托管堆分配"]',
      '    B --> C["对象仍可达"]',
      '    C --> D["继续存活"]',
      '    C --> E["不可达"]',
      '    E --> F["GC 标记 / 清理 / 压缩"]',
      '```',
      '',
      '## 为什么是追踪式 GC',
      '',
      '引用计数无法很好处理循环引用。CLR 使用追踪式 GC：从根对象出发，找到仍然可达的对象，其余对象就可以回收。',
      '',
      '代际回收有效，是因为大部分对象生命周期很短。第 0 代频繁回收，长寿对象才会晋升到更老的代。',
      '',
      '## 实践建议',
      '',
      '- 不要在热点循环里制造大量临时对象。',
      '- 不要让静态集合长期持有本该释放的对象。',
      '- 文件句柄、网络连接等非托管资源要及时 `Dispose`。',
      '- GC 管内存，不等于帮你管理所有资源生命周期。',
    ]),
    enBody: b([
      '# .NET Managed Heap and Garbage Collection: How Object Lifetime Is Managed',
      '',
      '`new` does more than create an instance. It asks the CLR to allocate an object on the managed heap. Allocation is often fast, but collection, compaction, and promotion still have runtime costs.',
      '',
      '## Why Tracing GC',
      '',
      'Reference counting struggles with cycles. The CLR uses tracing GC: it starts from roots, finds reachable objects, and collects the rest.',
      '',
      'Generational GC works because most objects are short-lived. Gen 0 is collected frequently, while long-lived objects are promoted to older generations.',
      '',
      '## Practical Advice',
      '',
      '- Avoid creating many temporary objects in hot loops.',
      '- Do not let static collections hold objects forever by accident.',
      '- Dispose unmanaged resources such as file handles and network connections promptly.',
      '- GC manages memory; it does not automatically manage every resource lifetime.',
    ]),
  },
  {
    groupId: 'csharp-equals-gethashcode-and-hash-collections',
    tags: ['csharp', 'dotnet'],
    zhTitle: 'C# Equals、GetHashCode 与哈希集合语义',
    enTitle: 'C# Equals, GetHashCode, and Hash Collection Semantics',
    zhSummary: '解释为什么自定义相等性时必须同时处理 Equals 和 GetHashCode，以及哈希集合如何使用它们。',
    enSummary: 'Why custom equality must handle both Equals and GetHashCode, and how hash-based collections use them.',
    zhBody: b([
      '# C# Equals、GetHashCode 与哈希集合语义',
      '',
      '如果对象要进入 `Dictionary`、`HashSet` 这类结构，就不能只改 `Equals` 而不改 `GetHashCode`。',
      '',
      '哈希容器通常先用哈希值定位桶，再用相等性确认元素。如果两个逻辑相等的对象哈希值不同，就可能出现查找失败、删除失败或重复元素。',
      '',
      '## 最佳实践',
      '',
      '- 自定义值相等时，同时实现 `Equals` 和 `GetHashCode`。',
      '- 作为哈希键的对象最好不可变。',
      '- 不要用随机数或会变化的字段生成哈希值。',
      '- 能用 record 表达值对象时，可以优先考虑 record。',
    ]),
    enBody: b([
      '# C# Equals, GetHashCode, and Hash Collection Semantics',
      '',
      'If an object is used in `Dictionary` or `HashSet`, overriding only `Equals` is not enough. `GetHashCode` must match the equality semantics.',
      '',
      'Hash collections usually locate a bucket by hash code first, then confirm equality. If two logically equal objects produce different hash codes, lookup, removal, or de-duplication may fail.',
      '',
      '## Best Practices',
      '',
      '- Implement `Equals` and `GetHashCode` together when defining value equality.',
      '- Prefer immutable objects as hash keys.',
      '- Do not generate hash codes from random or mutable state.',
      '- Consider records when they fit value-object semantics.',
    ]),
  },
  {
    groupId: 'csharp-floating-point-decimal-and-business-precision',
    tags: ['csharp', 'dotnet'],
    zhTitle: 'C# double、decimal 与业务精度选择',
    enTitle: 'C# double, decimal, and Business Precision',
    zhSummary: '解释二进制浮点为什么会有精度误差，以及金额、折扣、统计场景应该如何选择数值类型。',
    enSummary: 'Why binary floating point has precision issues, and how to choose numeric types for money, discounts, and approximate calculation.',
    zhBody: b([
      '# C# double、decimal 与业务精度选择',
      '',
      '`double` 采用二进制浮点表示，适合科学计算和近似值，但不是所有十进制小数都能被二进制精确表示。',
      '',
      '```csharp',
      'double a = 0.1 + 0.2;',
      'Console.WriteLine(a == 0.3); // False',
      '',
      'decimal price = 0.1m + 0.2m;',
      'Console.WriteLine(price == 0.3m); // True',
      '```',
      '',
      '## 怎么选',
      '',
      '- 金额、折扣、税率：优先 `decimal`。',
      '- 图形、物理仿真、统计近似：通常使用 `double`。',
      '- 需要精确等于的业务判断，不要直接依赖浮点比较。',
    ]),
    enBody: b([
      '# C# double, decimal, and Business Precision',
      '',
      '`double` uses binary floating-point representation. It is good for scientific and approximate calculations, but not every decimal fraction can be represented exactly in binary.',
      '',
      '## How to Choose',
      '',
      '- Money, discounts, and tax rates: prefer `decimal`.',
      '- Graphics, physics simulation, and statistical approximation: `double` is common.',
      '- Business rules that require exact equality should not rely on direct floating-point comparison.',
    ]),
  },
  {
    groupId: 'csharp-conditional-attribute-for-debug-diagnostics',
    tags: ['csharp', 'dotnet'],
    zhTitle: 'C# ConditionalAttribute：让调试诊断逻辑不污染主流程',
    enTitle: 'C# ConditionalAttribute: Keeping Debug Diagnostics out of the Main Flow',
    zhSummary: '说明 ConditionalAttribute 适合哪些调试、日志和诊断调用，以及它和 #if 的边界。',
    enSummary: 'How ConditionalAttribute helps with debug, logging, and diagnostic calls, and where its boundary differs from #if.',
    zhBody: b([
      '# C# ConditionalAttribute：让调试诊断逻辑不污染主流程',
      '',
      '`#if/#endif` 很直接，但容易把函数体切得支离破碎。`ConditionalAttribute` 适合把条件控制放到方法级别，让主流程保持干净。',
      '',
      '## 适用场景',
      '',
      '- 调试日志。',
      '- 诊断埋点。',
      '- 只在特定编译符号下保留的轻量追踪。',
      '',
      '## 注意边界',
      '',
      '`Conditional` 用在方法上，通常适合 `void` 方法。它不是任意条件编译的替代品，而是让辅助调用是否进入程序集变得更集中。',
    ]),
    enBody: b([
      '# C# ConditionalAttribute: Keeping Debug Diagnostics out of the Main Flow',
      '',
      '`#if/#endif` is direct, but it can fragment method bodies. `ConditionalAttribute` moves the condition to the method level and keeps the main flow cleaner.',
      '',
      '## Good Use Cases',
      '',
      '- Debug logging.',
      '- Diagnostic instrumentation.',
      '- Lightweight tracing that should exist only under specific compilation symbols.',
    ]),
  },
  {
    groupId: 'dotnet-thread-costs-threadpool-and-async-boundaries',
    tags: ['dotnet', 'csharp'],
    zhTitle: '.NET 线程成本、线程池与异步边界',
    enTitle: '.NET Thread Costs, Thread Pool, and Async Boundaries',
    zhSummary: '从线程空间成本、调度成本到线程池和 async，理解为什么现代 .NET 不应该随意创建线程。',
    enSummary: 'From stack and scheduling cost to the thread pool and async boundaries, understand why modern .NET should not create threads casually.',
    zhBody: b([
      '# .NET 线程成本、线程池与异步边界',
      '',
      '线程让程序可以在等待 I/O、执行计算、响应请求时并发推进，但线程不是免费资源。每个线程都有栈、内核结构和调度成本。',
      '',
      '现代 .NET 更常用线程池、Task 和 async I/O，而不是手工创建大量线程。线程仍然存在，只是不应该直接成为业务表达方式。',
      '',
      '## 判断方式',
      '',
      '- CPU 密集任务要控制并行度。',
      '- I/O 密集任务优先使用 async。',
      '- 不要把 `Thread.Sleep` 当作异步等待。',
      '- 线程池耗尽通常会表现为延迟升高，而不是明显报错。',
    ]),
    enBody: b([
      '# .NET Thread Costs, Thread Pool, and Async Boundaries',
      '',
      'Threads let programs make progress while waiting for IO, doing computation, or serving requests, but they are not free. Each thread has stack memory, kernel structures, and scheduling cost.',
      '',
      'Modern .NET usually uses the thread pool, tasks, and async IO instead of creating many threads manually.',
    ]),
  },
  {
    groupId: 'csharp-locks-deadlock-and-shared-state-governance',
    tags: ['csharp', 'dotnet'],
    zhTitle: 'C# lock、死锁与共享状态治理',
    enTitle: 'C# lock, Deadlocks, and Shared-State Governance',
    zhSummary: '聚焦 lock 的正确用法、死锁形成条件，以及如何通过锁顺序和临界区设计降低风险。',
    enSummary: 'A focused note on using lock correctly, how deadlocks form, and how lock ordering and critical-section design reduce risk.',
    zhBody: b([
      '# C# lock、死锁与共享状态治理',
      '',
      '`lock` 的价值不是加速，而是保护共享状态的一致性。它让同一段临界区在同一时刻只被一个线程进入。',
      '',
      '## 死锁为什么出现',
      '',
      '死锁通常来自循环等待：线程 1 拿着锁 A 等锁 B，线程 2 拿着锁 B 等锁 A。',
      '',
      '## 实践规则',
      '',
      '- 锁对象保持私有。',
      '- 不锁 `this`、字符串、`typeof(...)`。',
      '- 临界区尽量短。',
      '- 不在锁里做外部调用或阻塞 I/O。',
      '- 多锁场景必须定义统一加锁顺序。',
    ]),
    enBody: b([
      '# C# lock, Deadlocks, and Shared-State Governance',
      '',
      'The value of `lock` is not speed. It protects shared-state consistency by allowing only one thread to enter a critical section at a time.',
      '',
      'Deadlocks often come from circular waiting: thread 1 holds lock A and waits for B, while thread 2 holds lock B and waits for A.',
    ]),
  },
  {
    groupId: 'dotnet-producer-consumer-with-blockingcollection-and-backpressure',
    tags: ['dotnet', 'csharp'],
    zhTitle: '.NET BlockingCollection 与生产者消费者背压',
    enTitle: '.NET BlockingCollection and Producer-Consumer Backpressure',
    zhSummary: '把 BlockingCollection 放回生产者/消费者模型里理解：容量、阻塞、取消和背压。',
    enSummary: 'Understand BlockingCollection through the producer-consumer model: capacity, blocking, cancellation, and backpressure.',
    zhBody: b([
      '# .NET BlockingCollection 与生产者消费者背压',
      '',
      '`BlockingCollection<T>` 适合把“产生任务”和“处理任务”拆开。它的核心价值不是只有线程安全，而是让容量和背压变成显式设计。',
      '',
      '## 适用场景',
      '',
      '- 批处理任务队列。',
      '- 需要限流的后台工作器。',
      '- 生产速度可能高于消费速度，需要阻塞或背压。',
      '',
      '即使新项目更多使用 `Channel<T>`，理解 `BlockingCollection<T>` 仍然能帮助你理解队列、容量、取消和吞吐之间的关系。',
    ]),
    enBody: b([
      '# .NET BlockingCollection and Producer-Consumer Backpressure',
      '',
      '`BlockingCollection<T>` separates producing work from consuming work. Its main value is not only thread safety; it makes capacity and backpressure explicit.',
    ]),
  },
  {
    groupId: 'aspnet-core-dependency-injection-lifetimes',
    tags: ['aspnet-core', 'dotnet'],
    zhTitle: 'ASP.NET Core 依赖注入与生命周期选择',
    enTitle: 'ASP.NET Core Dependency Injection and Lifetime Selection',
    zhSummary: '聚焦 DI 的价值，以及 Singleton、Scoped、Transient 三种生命周期的选择和常见坑。',
    enSummary: 'A focused note on DI value, lifetime selection, and common traps around Singleton, Scoped, and Transient services.',
    zhBody: b([
      '# ASP.NET Core 依赖注入与生命周期选择',
      '',
      '依赖注入的核心不是框架魔法，而是把对象创建和对象使用分开。业务代码依赖接口，容器负责装配实现。',
      '',
      '## 生命周期',
      '',
      '- `Singleton`：整个应用一个实例。',
      '- `Scoped`：每个请求或作用域一个实例。',
      '- `Transient`：每次解析都新建。',
      '',
      '最常见的问题不是不会注册，而是生命周期搭配错误。例如单例服务捕获 scoped 服务，会把请求级状态意外拉长。',
    ]),
    enBody: b([
      '# ASP.NET Core Dependency Injection and Lifetime Selection',
      '',
      'The core idea of dependency injection is separating object creation from object usage. Business code depends on interfaces while the container assembles implementations.',
    ]),
  },
  {
    groupId: 'mediatr-request-handlers-and-pipeline-behaviors',
    tags: ['dotnet', 'aspnet-core'],
    zhTitle: 'MediatR 请求处理与 Pipeline Behavior 边界',
    enTitle: 'MediatR Request Handlers and Pipeline Behavior Boundaries',
    zhSummary: '把 MediatR 用在请求和处理解耦，而不是把简单 CRUD 包成过度抽象。',
    enSummary: 'Use MediatR to decouple requests and handlers without turning simple CRUD into unnecessary abstraction.',
    zhBody: b([
      '# MediatR 请求处理与 Pipeline Behavior 边界',
      '',
      'MediatR 的价值是让请求和处理解耦。调用者发送命令或查询，具体处理由 Handler 承担。',
      '',
      '## 适合场景',
      '',
      '- 控制器需要变薄。',
      '- 一个入口后面有清晰的业务流程。',
      '- 想统一加日志、验证、事务、计时等 Pipeline Behavior。',
      '',
      '## 不适合场景',
      '',
      '- 极简单 CRUD。',
      '- 团队还不理解中介者模式。',
      '- 只是为了“看起来架构好”而引入。',
    ]),
    enBody: b([
      '# MediatR Request Handlers and Pipeline Behavior Boundaries',
      '',
      'MediatR decouples a request from its handler. A caller sends a command or query, and a handler owns the processing.',
    ]),
  },
  {
    groupId: 'dotnet-aop-with-castle-dynamicproxy',
    tags: ['dotnet', 'csharp'],
    zhTitle: '.NET AOP 与 Castle DynamicProxy：横切逻辑怎么落地',
    enTitle: '.NET AOP with Castle DynamicProxy: Implementing Cross-Cutting Concerns',
    zhSummary: '把日志、审计、缓存、性能统计等横切逻辑放到代理层，但不要把核心业务藏进去。',
    enSummary: 'Move logging, audit, caching, and timing concerns into proxy layers without hiding core business rules there.',
    zhBody: b([
      '# .NET AOP 与 Castle DynamicProxy：横切逻辑怎么落地',
      '',
      'AOP 适合处理横切关注点：日志、审计、缓存、性能统计。Castle DynamicProxy 可以通过拦截器把这些逻辑提到代理层。',
      '',
      '## 适合放进拦截器的内容',
      '',
      '- 请求前后的统一日志。',
      '- 统一审计。',
      '- 缓存或性能统计。',
      '- 测试中的替身和模拟对象。',
      '',
      '不要把核心业务规则藏进拦截器。否则代码会变得不透明，排查问题时很难看清真实调用链。',
    ]),
    enBody: b([
      '# .NET AOP with Castle DynamicProxy: Implementing Cross-Cutting Concerns',
      '',
      'AOP is useful for cross-cutting concerns: logging, audit, caching, and timing. Castle DynamicProxy can move this behavior into interceptors.',
    ]),
  },
  {
    groupId: 'resilient-http-calls-with-httpclientfactory-and-polly',
    tags: ['dotnet', 'aspnet-core'],
    zhTitle: 'HttpClientFactory 与 Polly：让外部 HTTP 调用更稳',
    enTitle: 'HttpClientFactory and Polly: Making External HTTP Calls More Resilient',
    zhSummary: '聚焦 HttpClient 生命周期、类型化客户端、超时、重试、熔断与降级的组合设计。',
    enSummary: 'A focused note on HttpClient lifetime, typed clients, timeout, retry, circuit breaker, and fallback design.',
    zhBody: b([
      '# HttpClientFactory 与 Polly：让外部 HTTP 调用更稳',
      '',
      '`HttpClientFactory` 解决的是 `HttpClient` 使用方式问题：连接管理、Handler 生命周期、DNS 更新、命名客户端和类型化客户端。',
      '',
      'Polly 负责把韧性策略显式化：Retry、Circuit Breaker、Timeout、Bulkhead、Fallback。',
      '',
      '## 原则',
      '',
      '- 只对瞬时故障重试。',
      '- 重试前确认幂等性。',
      '- 不要无限重试。',
      '- 超时、重试、熔断要一起设计。',
      '- 降级结果要对调用方可见。',
    ]),
    enBody: b([
      '# HttpClientFactory and Polly: Making External HTTP Calls More Resilient',
      '',
      '`HttpClientFactory` solves usage problems around `HttpClient`: connection management, handler lifetime, DNS refresh, named clients, and typed clients.',
    ]),
  },
  {
    groupId: 'tcp-connection-lifecycle-handshake-and-teardown',
    tags: ['dotnet', 'system-design'],
    zhTitle: 'TCP 连接生命周期：三次握手、四次挥手与 TIME_WAIT',
    enTitle: 'TCP Connection Lifecycle: What Handshake and Teardown Solve',
    zhSummary: '从可靠双向连接的角度理解 TCP 三次握手、四次挥手、半关闭、TIME_WAIT，以及排查连接问题时应该看什么。',
    enSummary: 'Understand TCP three-way handshake, four-way teardown, half-close, TIME_WAIT, and what to inspect when debugging connection issues.',
    zhBody: b([
      '# TCP 连接生命周期：三次握手、四次挥手与 TIME_WAIT',
      '',
      'TCP 的目标不是“尽快发出去”，而是可靠地建立一条可用的双向字节流。所谓连接生命周期，本质上是在解决三件事：双方是否都能收发、序列号从哪里开始、连接如何有序关闭。',
      '',
      '如果只记住 SYN、ACK、FIN，很容易把它当成背诵题。更好的理解方式是：TCP 一直在维护“两个方向的数据流”和“双方各自的状态机”。',
      '',
      '## 建立连接前，双方要确认什么',
      '',
      '客户端和服务端在正式传数据之前，至少要确认四件事：',
      '',
      '- 客户端能发，服务端能收。',
      '- 服务端能发，客户端能收。',
      '- 双方的初始序列号已经交换。',
      '- 双方都知道连接已经进入可用状态。',
      '',
      '```mermaid',
      'sequenceDiagram',
      '    participant C as Client',
      '    participant S as Server',
      '    C->>S: SYN, seq=x',
      '    S-->>C: SYN + ACK, seq=y, ack=x+1',
      '    C->>S: ACK, ack=y+1',
      '```',
      '',
      '## 三次握手每一步解决什么',
      '',
      '第一次握手：客户端发送 SYN，告诉服务端“我想建立连接，我的初始序列号是 x”。这一步只能证明客户端能发到服务端。',
      '',
      '第二次握手：服务端返回 SYN + ACK，告诉客户端“我收到了你的 SYN，我的初始序列号是 y”。这一步证明服务端能收，也能发回客户端。',
      '',
      '第三次握手：客户端返回 ACK，告诉服务端“我也收到了你的 SYN”。到这里，服务端才知道自己的发送能力也被客户端确认了。',
      '',
      '所以三次握手不是为了凑次数，而是为了完成双向收发能力和初始序列号的确认。',
      '',
      '## 为什么不是两次握手',
      '',
      '如果只有两次，服务端无法确认客户端是否真的收到了自己的 SYN + ACK。更麻烦的是，网络里可能存在延迟到达的旧 SYN。两次握手更容易让服务端误以为一个过期连接仍然有效。',
      '',
      '第三次 ACK 的意义，是让服务端确认“客户端已经收到我的确认，并且愿意进入连接状态”。',
      '',
      '## 数据传输时，ACK 在确认什么',
      '',
      'TCP 是字节流协议，ACK 确认的是“下一个期望收到的字节序号”，不是某个业务消息。应用层的一次写入，不一定等于网络上的一个包；网络上的多个包，也可能被应用层一次读取到。',
      '',
      '这也是为什么业务协议需要自己设计消息边界，例如长度前缀、分隔符或固定格式。',
      '',
      '## 为什么断开通常是四次挥手',
      '',
      'TCP 是全双工连接。客户端停止发送，不代表服务端也已经发送完。关闭连接时，两个方向需要分别关闭。',
      '',
      '```mermaid',
      'sequenceDiagram',
      '    participant A as 主动关闭方',
      '    participant B as 被动关闭方',
      '    A->>B: FIN',
      '    B-->>A: ACK',
      '    B-->>A: FIN',
      '    A->>B: ACK',
      '```',
      '',
      '第一步 FIN 表示“我这边没有数据要发了”。对方 ACK 后，只代表它确认了这个方向关闭。被动关闭方可能还有数据要发，所以它会等自己发送完，再发出自己的 FIN。',
      '',
      '这就是四次挥手的核心：两个方向分别结束。',
      '',
      '## TIME_WAIT 为什么存在',
      '',
      '主动关闭方发出最后一个 ACK 后，通常会进入 TIME_WAIT。它看起来像“连接已经结束但端口还被占着”，但它有两个重要作用：',
      '',
      '- 确保最后一个 ACK 如果丢失，对方重发 FIN 时还能再次 ACK。',
      '- 等待旧连接中的延迟报文在网络中自然消失，避免污染后续同四元组连接。',
      '',
      'TIME_WAIT 不是异常，而是 TCP 为可靠关闭付出的成本。服务端如果出现大量 TIME_WAIT，要先看是谁主动关闭连接，以及连接复用、Keep-Alive、连接池配置是否合理。',
      '',
      '## 和应用开发最相关的排查点',
      '',
      '- 连接建立慢：关注 DNS、网络延迟、SYN 重传、服务端 backlog。',
      '- 偶发连接失败：关注防火墙、端口耗尽、连接池上限、服务端 accept 能力。',
      '- 大量 TIME_WAIT：关注主动关闭方、短连接比例、HTTP Keep-Alive。',
      '- 大量 CLOSE_WAIT：通常说明应用收到了对方 FIN，但自己没有及时关闭 socket。',
      '- 请求读写异常：区分 TCP 连接问题和应用层协议边界问题。',
      '',
      '## 小结',
      '',
      '三次握手解决的是连接建立前的双向确认和初始序列号同步。四次挥手解决的是全双工连接的双向关闭。TIME_WAIT 解决的是最后确认和旧报文消散问题。',
      '',
      '理解这些状态，不是为了在面试里背流程，而是为了在服务通信、连接池、网关、gRPC、HTTP API 排查时能知道自己到底在看哪一层。',
    ]),
    enBody: b([
      '# TCP Connection Lifecycle: What Handshake and Teardown Solve',
      '',
      'TCP is not about sending as quickly as possible. It establishes a reliable bidirectional byte stream. The connection lifecycle answers three questions: whether both sides can send and receive, where sequence numbers start, and how both directions close cleanly.',
      '',
      '## What Must Be Confirmed Before Data Transfer',
      '',
      'Before application data flows, both sides need to confirm that the client can reach the server, the server can reach the client, initial sequence numbers are exchanged, and both sides agree that the connection is usable.',
      '',
      '```mermaid',
      'sequenceDiagram',
      '    participant C as Client',
      '    participant S as Server',
      '    C->>S: SYN, seq=x',
      '    S-->>C: SYN + ACK, seq=y, ack=x+1',
      '    C->>S: ACK, ack=y+1',
      '```',
      '',
      '## Why Three Steps',
      '',
      'The first SYN proves that the client can send to the server. The SYN+ACK proves that the server received the SYN and can send back. The final ACK proves that the client received the server side of the handshake.',
      '',
      'Three steps are not arbitrary. They complete bidirectional reachability confirmation and initial sequence-number synchronization.',
      '',
      '## Why Not Two Steps',
      '',
      'With only two steps, the server cannot know whether the client received its SYN+ACK. Delayed old SYN packets can also make stale connection attempts look valid. The final ACK tells the server that the client really saw the server response and is ready to enter the established state.',
      '',
      '## What ACK Means During Data Transfer',
      '',
      'TCP is a byte-stream protocol. ACK confirms the next expected byte sequence number, not an application message. One application write is not necessarily one packet, and multiple packets may be read by the application at once.',
      '',
      'That is why application protocols need their own message boundaries, such as length prefixes, delimiters, or fixed formats.',
      '',
      '## Why Teardown Often Takes Four Steps',
      '',
      'TCP is full duplex. One side finishing its sends does not mean the other side is done. Each direction closes independently.',
      '',
      '```mermaid',
      'sequenceDiagram',
      '    participant A as Active closer',
      '    participant B as Passive closer',
      '    A->>B: FIN',
      '    B-->>A: ACK',
      '    B-->>A: FIN',
      '    A->>B: ACK',
      '```',
      '',
      'FIN means “I have no more data to send.” The ACK only confirms that one direction has closed. The passive side may still have data to send, so it sends its own FIN later.',
      '',
      '## Why TIME_WAIT Exists',
      '',
      'After the active closer sends the final ACK, it usually enters TIME_WAIT. This state has two purposes: it allows the final ACK to be retransmitted if the peer resends FIN, and it lets delayed packets from the old connection disappear from the network.',
      '',
      'TIME_WAIT is not automatically a bug. If there are many TIME_WAIT connections, first check who actively closes connections, whether short connections dominate, and whether Keep-Alive or connection pooling is configured correctly.',
      '',
      '## Debugging View',
      '',
      '- Slow connection setup: inspect DNS, latency, SYN retransmission, and server backlog.',
      '- Intermittent connection failures: inspect firewall rules, ephemeral port exhaustion, pool limits, and accept capacity.',
      '- Many TIME_WAIT sockets: inspect active closer behavior, short-connection ratio, and HTTP Keep-Alive.',
      '- Many CLOSE_WAIT sockets: the application likely received FIN but did not close its socket.',
      '- Read/write surprises: distinguish TCP connection behavior from application protocol framing.',
      '',
      '## Summary',
      '',
      'The three-way handshake establishes bidirectional confirmation and sequence synchronization. The four-way teardown closes both directions of a full-duplex connection. TIME_WAIT protects the final ACK and prevents delayed old packets from polluting later connections.',
    ]),
  },
  {
    groupId: 'wcf-as-legacy-dotnet-service-communication',
    tags: ['dotnet', 'architecture'],
    zhTitle: 'WCF 作为 .NET 服务通信历史资产该怎么看',
    enTitle: 'How to Treat WCF as a Legacy .NET Service Communication Asset',
    zhSummary: 'WCF 曾经统一 SOAP、Remoting、消息队列和服务契约；今天更适合作为历史资产来评估迁移边界。',
    enSummary: 'WCF once unified SOAP, Remoting, queues, and service contracts; today it is better treated as a legacy asset with explicit migration boundaries.',
    zhBody: b([
      '# WCF 作为 .NET 服务通信历史资产该怎么看',
      '',
      'WCF 的出现，是为了统一当时 .NET 世界里分散的通信技术：ASP.NET Web Service、.NET Remoting、消息队列、SOAP/WSDL 风格服务。',
      '',
      '它的能力很强，但也有明显时代特征：配置复杂、学习成本高、和现代轻量 HTTP API 的使用习惯不同。',
      '',
      '## 今天如何处理',
      '',
      '- 维护系统时先识别契约和调用方。',
      '- 不要为了“现代化”盲目重写。',
      '- 新功能优先放到更轻量的 HTTP API 或 gRPC 边界。',
      '- 迁移时先切边界，再替换实现。',
    ]),
    enBody: b([
      '# How to Treat WCF as a Legacy .NET Service Communication Asset',
      '',
      'WCF was created to unify scattered communication technologies in the .NET ecosystem: ASP.NET Web Services, .NET Remoting, queues, and SOAP/WSDL-style services.',
    ]),
  },
  {
    groupId: 'grpc-contract-first-service-communication-in-dotnet',
    tags: ['dotnet', 'microservices'],
    zhTitle: 'gRPC 在 .NET 中的契约优先服务通信',
    enTitle: 'Contract-First Service Communication with gRPC in .NET',
    zhSummary: '解释 gRPC 为什么适合内部服务调用、强契约通信和流式传输，以及它不适合的边界。',
    enSummary: 'Why gRPC fits internal service calls, strong contracts, and streaming, and where it may not be the right choice.',
    zhBody: b([
      '# gRPC 在 .NET 中的契约优先服务通信',
      '',
      'gRPC 建立在 HTTP/2 和 Protocol Buffers 上，适合服务间调用、强契约通信和流式传输。',
      '',
      '## 它解决什么',
      '',
      '- 二进制序列化比 JSON 更紧凑。',
      '- HTTP/2 支持多路复用和流式传输。',
      '- `.proto` 让接口契约明确，代码生成减少样板。',
      '',
      '它适合内部微服务、高性能调用、强契约系统。不一定适合公开浏览器 API、极简调试场景或团队还不熟悉 proto/HTTP2 的项目。',
    ]),
    enBody: b([
      '# Contract-First Service Communication with gRPC in .NET',
      '',
      'gRPC is built on HTTP/2 and Protocol Buffers. It fits service-to-service calls, strong contracts, and streaming.',
    ]),
  },
  {
    groupId: 'jwt-authentication-and-token-validation-in-dotnet-apis',
    tags: ['dotnet', 'aspnet-core'],
    zhTitle: 'JWT 认证与 .NET API Token 校验要点',
    enTitle: 'JWT Authentication and Token Validation in .NET APIs',
    zhSummary: '把 JWT 拆成签名、声明、过期、issuer、audience 等校验点，避免把“已签名”误解成“已加密”。',
    enSummary: 'Break JWT validation into signature, claims, expiration, issuer, and audience checks, and avoid confusing signed with encrypted.',
    zhBody: b([
      '# JWT 认证与 .NET API Token 校验要点',
      '',
      'JWT 的关键不是把字符串变短，而是把声明封装成可签名、可验证的令牌。已签名不等于已加密，Payload 里不应该放机密。',
      '',
      '## 必须校验什么',
      '',
      '- 签名是否有效。',
      '- issuer 是否可信。',
      '- audience 是否匹配。',
      '- token 是否过期。',
      '- 刷新策略是否合理。',
      '',
      '上线时还要考虑密钥管理、算法选择、撤销策略和权限声明最小化。',
    ]),
    enBody: b([
      '# JWT Authentication and Token Validation in .NET APIs',
      '',
      'JWT is not mainly about making strings shorter. It packages claims into a signed and verifiable token. Signed does not mean encrypted, so the payload should not contain secrets.',
    ]),
  },
  {
    groupId: 'dotnet-cli-daily-development-workflow',
    tags: ['dotnet'],
    zhTitle: 'dotnet CLI 日常开发工作流',
    enTitle: 'Daily Development Workflow with the dotnet CLI',
    zhSummary: '把 dotnet new、restore、build、test、run、publish 串成本地开发和 CI 都能复用的统一入口。',
    enSummary: 'Connect dotnet new, restore, build, test, run, and publish into a workflow shared by local development and CI.',
    zhBody: b([
      '# dotnet CLI 日常开发工作流',
      '',
      '`.NET CLI` 不只是 IDE 的命令行替代品，而是工程化流程的统一入口：创建、恢复、编译、测试、运行、发布。',
      '',
      '```bash',
      'dotnet new webapi -n Demo.Api',
      'dotnet restore',
      'dotnet build',
      'dotnet test',
      'dotnet run',
      'dotnet publish -c Release -o ./publish',
      '```',
      '',
      '它的价值是让本地和 CI 使用同一套命令，让构建、测试、发布动作更容易脚本化。',
    ]),
    enBody: b([
      '# Daily Development Workflow with the dotnet CLI',
      '',
      'The .NET CLI is not only an IDE replacement. It is a unified entry point for creating, restoring, building, testing, running, and publishing projects.',
    ]),
  },
  {
    groupId: 'nuget-source-management-and-private-package-governance',
    tags: ['dotnet'],
    zhTitle: 'NuGet 源管理与私有包治理',
    enTitle: 'NuGet Source Management and Private Package Governance',
    zhSummary: '聚焦 nuget.config、私有源、包删除/取消列出、凭据和依赖混淆风险。',
    enSummary: 'A focused note on nuget.config, private feeds, package unlisting, credentials, and dependency-confusion risk.',
    zhBody: b([
      '# NuGet 源管理与私有包治理',
      '',
      'NuGet 源管理解决的是：包从哪里来、是否可信、团队如何统一配置。',
      '',
      '## 风险',
      '',
      '- 源过多导致依赖混淆。',
      '- 凭据管理不当带来安全风险。',
      '- 包命名不规范导致升级困难。',
      '- 删除或取消列出包前没有确认下游依赖。',
      '',
      '私有源应该通过 `nuget.config` 统一管理，不要把地址散落在脚本里。',
    ]),
    enBody: b([
      '# NuGet Source Management and Private Package Governance',
      '',
      'NuGet source management answers where packages come from, whether they are trusted, and how a team configures feeds consistently.',
    ]),
  },
];

const db = new Database(dbPath);

const deleteOldPost = db.prepare('delete from blog_posts where group_id = ?');
const upsertPost = db.prepare(`
  insert into blog_posts (
    id,
    group_id,
    category,
    hero_image,
    updated_at,
    tag_ids_json,
    published,
    status,
    series,
    slug_zh,
    title_zh,
    summary_zh,
    body_zh,
    slug_en,
    title_en,
    summary_en,
    body_en
  ) values (
    @id,
    @groupId,
    'dotnet-core',
    '/assets/architecture/content-platform.svg',
    @updatedAt,
    @tagIdsJson,
    1,
    'published',
    'dotnet-core',
    @slugZh,
    @titleZh,
    @summaryZh,
    @bodyZh,
    @slugEn,
    @titleEn,
    @summaryEn,
    @bodyEn
  )
  on conflict(group_id) do update set
    category = excluded.category,
    hero_image = excluded.hero_image,
    updated_at = excluded.updated_at,
    tag_ids_json = excluded.tag_ids_json,
    published = excluded.published,
    status = excluded.status,
    series = excluded.series,
    slug_zh = excluded.slug_zh,
    title_zh = excluded.title_zh,
    summary_zh = excluded.summary_zh,
    body_zh = excluded.body_zh,
    slug_en = excluded.slug_en,
    title_en = excluded.title_en,
    summary_en = excluded.summary_en,
    body_en = excluded.body_en
`);

const transaction = db.transaction(() => {
  for (const groupId of oldCombinedPostIds) {
    deleteOldPost.run(groupId);
  }

  for (const post of posts) {
    upsertPost.run({
      id: `blog-${post.groupId}`,
      groupId: post.groupId,
      updatedAt,
      tagIdsJson: JSON.stringify(post.tags),
      slugZh: post.groupId,
      titleZh: post.zhTitle,
      summaryZh: post.zhSummary,
      bodyZh: post.zhBody,
      slugEn: post.groupId,
      titleEn: post.enTitle,
      summaryEn: post.enSummary,
      bodyEn: post.enBody,
    });
  }
});

transaction();
db.close();

console.log(`Seeded ${posts.length} split .NET blog posts into ${dbPath}`);
